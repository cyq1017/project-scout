interface:
  display_name: "Prior Art Scout"
  short_description: "Evidence-gated build-vs-adopt due diligence"
  default_prompt: "Use $prior-art-scout to run local-first, evidence-gated build-vs-adopt due diligence; record external repo follow-ups without operating them."

policy:
  allow_implicit_invocation: true
